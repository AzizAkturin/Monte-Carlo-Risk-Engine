__future__ is a special standard-library module that lets you opt in to language features from a newer Python version while still running an older one
Sometimes Python introduces a change that:
    is better / more consistent, but
    would break old code if turned on immediately.